@%@UCRWARNING=# @%@

PATH=/usr/sbin:/usr/bin:/sbin:/bin

@!@
disabled = ''
if not configRegistry.is_true('nss/passwd/cachefile', False):
	disabled = '# '
interval = configRegistry.get('nss/passwd/cachefile/invalidate_interval', '*/15 * * * *')
if interval == 'disabled':
	print '# /usr/lib/univention-ucc-application-server/ldap-passwd-to-file.py is disbaled via UCR variable nss/passwd/cachefile/invalidate_interval'
else:
	print '%s%s   root   [ -x /usr/lib/univention-ucc-application-server/ldap-passwd-to-file.py ] && /usr/lib/univention-ucc-application-server/ldap-passwd-to-file.py' % (disabled, interval)
@!@
